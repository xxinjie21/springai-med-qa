/**
 * Domain layer: entities (session/message/audit), enums (role/status) and request/response
 * DTOs. Entity fields are strictly aligned with the unified medical storage specification.
 */
package com.med.qa.domain;
